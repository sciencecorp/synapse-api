syntax = "proto3";

package synapse;

message OpticalBroadbandConfig {
  uint32 peripheral_id = 1;
  repeated uint32 pixel_mask = 2;
  uint32 bit_width = 3;
  uint32 frame_rate = 4;
  uint32 gain = 5;
}
